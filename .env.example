VITE_API_BASE_URL=http://localhost:3000/api/v1
# When API uses Cloudflare R2, media/thumbnail/release URLs are absolute HTTPS —
# no extra VITE var needed. Ensure R2 bucket CORS allows this admin origin.
# VITE_DISPLAY_TARGET=SCREEN_1
